---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ViewportAnimator from '../components/ViewportAnimator.astro';

const title = "Acerca de HYDRA²¹";
const description = "Conoce más sobre HYDRA²¹ y nuestro compromiso con la ingeniería hidráulica";
---

<Layout title={title} description={description}>
  <Header />

  <!-- About Hero Section -->
  <section class="about-hero-section relative min-h-screen flex items-center overflow-hidden bg-black">
    <!-- Background Image -->
    <div class="absolute inset-0 bg-black about-background">
      <div class="absolute inset-0 bg-black/40 xs:bg-black/30 sm:bg-black/25 md:bg-black/20 lg:bg-black/30"></div>
    </div>

    <!-- Top blur transition -->
    <div class="absolute top-0 left-0 right-0 h-20 sm:h-24 md:h-32 lg:h-40 bg-gradient-to-b from-black via-black/80 to-transparent z-20"></div>

    <!-- Bottom blur transition -->
    <div class="absolute bottom-0 left-0 right-0 h-20 sm:h-24 md:h-32 lg:h-40 bg-gradient-to-t from-black via-black/80 to-transparent z-20"></div>

    <!-- Geometric grid pattern -->
    <div class="absolute inset-0 opacity-4 xs:opacity-5 sm:opacity-6 md:opacity-8 lg:opacity-10">
      <div class="absolute inset-0 grid-pattern"></div>
    </div>

    <!-- Main content container following Hero patterns -->
    <div class="container mx-auto px-4 py-6 md:py-12 lg:py-16 relative z-10">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center justify-items-center lg:justify-items-stretch">

        <!-- Left side - Content -->
        <div class="space-y-4 md:space-y-6 lg:space-y-8 w-full lg:w-auto text-center lg:text-left">

          <!-- Badge: #INNOVACIÓN HIDRÁULICA -->
          <div class="inline-flex items-center gap-1 sm:gap-2 bg-gray-900/90 backdrop-blur-sm border border-blue-500/30 rounded-full px-2 py-1 text-xs sm:text-sm">
            <span class="text-orange-400 text-xs sm:text-sm">⚡</span>
            <span class="text-blue-400 font-medium text-xs sm:text-xs">#INNOVACIÓN HIDRÁULICA</span>
          </div>

          <!-- Main Heading -->
          <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-white leading-tight" transition:name="about-hero-title">
            Acerca de<br/>
            <span class="block text-blue-400" transition:name="logo-text">HYDRA²¹</span>
          </h1>

          <!-- Subtitle -->
          <p class="text-lg sm:text-xl text-gray-300 leading-relaxed max-w-2xl mx-auto lg:mx-0">
            Revolucionando la ingeniería hidráulica<br class="hidden sm:block"/>
            con tecnología de vanguardia.
          </p>

          <!-- Botones CTA en “About” (mismo estilo y animación que en el CTA principal) -->
          <div class="pt-4 md:pt-6 btn-group">
            <a
              href="/descargas"
              class="btn-modern-primary"
            >
              <!-- Windows Logo SVG -->
              <svg class="icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                <path d="M0 3.449L9.75 2.1v9.451H0m10.949-9.602L24 0v11.4H10.949M0 12.6h9.75v9.451L0 20.699M10.949 12.6H24V24l-13.051-1.351"/>
              </svg>
              Descargar HYDRA²¹
            </a>
            <a
              href="#mision"
              class="btn-modern-secondary"
            >
              <!-- Arrow Right SVG -->
              <svg class="icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
              </svg>
              Ver Características
            </a>
          </div>


          
        </div>

        <!-- Right side - About Image (solo ≥768px) -->
        <div class="hidden md:flex w-full lg:w-auto relative justify-center lg:justify-end items-center">
          <div class="relative w-80 h-80 md:w-96 md:h-96 lg:w-[28rem] lg:h-[28rem] xl:w-[32rem] xl:h-[32rem] 2xl:w-[36rem] 2xl:h-[36rem] flex items-center justify-center">
            <!-- Main about image -->
            <div class="relative">
              <img
                src="/assets/images/about.png"
                alt="Acerca de HYDRA²¹"
                class="w-full h-auto animate-float-gentle filter drop-shadow-2xl"
                transition:name="about-hero-image"
                loading="eager"
                decoding="async"
              />
            </div>

            <!-- Subtle glow effect -->
            <div class="absolute inset-0 bg-blue-500/10 rounded-2xl blur-3xl scale-110 opacity-50"></div>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- Mission Section -->
  <ViewportAnimator animation="fade-in" delay={100} duration={400}>
    <section id="mision" class="bg-black py-20 sm:py-24 md:py-28 lg:py-32">
      <div class="container mx-auto px-4 sm:px-6 lg:px-8">
        <div class="max-w-6xl mx-auto">

          <!-- Section Header -->
          <div class="text-center mb-16 sm:mb-20 md:mb-24">
            <h2 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight mb-6 sm:mb-8" transition:name="mission-title">
              Nuestra Misión
            </h2>
            <p class="text-lg sm:text-xl md:text-2xl text-gray-300 leading-relaxed max-w-4xl mx-auto">
              Transformando la ingeniería hidráulica con innovación y precisión
            </p>
          </div>

        <div class="grid lg:grid-cols-2 gap-8 sm:gap-10 md:gap-12 lg:gap-16 xl:gap-20 items-center">

          <!-- Mission Content Card -->
          <div class="group relative" transition:name="mission-content">
            <div class="relative h-full border-2 border-[rgba(14,165,233,0.3)] rounded-3xl bg-gradient-to-br from-[rgba(14,165,233,0.15)] via-[rgba(59,130,246,0.1)] to-[rgba(14,165,233,0.05)] backdrop-blur-xl p-6 sm:p-8 md:p-10 transition-all duration-500 hover:border-[rgba(14,165,233,0.6)] hover:shadow-2xl hover:shadow-blue-500/20">

              <!-- Glassmorphism overlay -->
              <div class="absolute inset-0 bg-gradient-to-br from-white/5 to-transparent rounded-3xl"></div>

              <div class="relative z-10 space-y-6">
                <div class="flex items-center gap-3 mb-6">
                  <div class="w-12 h-12 bg-blue-600/20 rounded-full flex items-center justify-center">
                    <span class="text-2xl">🎯</span>
                  </div>
                  <h3 class="text-xl sm:text-2xl font-bold text-white">Nuestra Visión</h3>
                </div>

                <p class="text-base sm:text-lg text-gray-300 leading-relaxed">
                  En HYDRA²¹, nos dedicamos a transformar la manera en que los ingenieros abordan los desafíos hidráulicos.
                  Nuestro software combina algoritmos avanzados con una interfaz intuitiva para ofrecer soluciones precisas
                  y eficientes.
                </p>

                <p class="text-base sm:text-lg text-gray-300 leading-relaxed">
                  Creemos que la tecnología debe empoderar a los profesionales, no complicar su trabajo. Por eso, cada
                  característica de HYDRA²¹ está diseñada pensando en la experiencia del usuario y la precisión técnica.
                </p>

                <!-- Mission highlights -->
                <div class="grid grid-cols-2 gap-3 sm:gap-4 mt-6 sm:mt-8">
                  <div class="text-center p-3 sm:p-4 bg-blue-600/10 rounded-xl border border-blue-500/20">
                    <div class="text-xl sm:text-2xl font-bold text-blue-400 mb-1">AI</div>
                    <div class="text-xs sm:text-sm text-gray-300">Inteligencia Artificial</div>
                  </div>
                  <div class="text-center p-3 sm:p-4 bg-cyan-600/10 rounded-xl border border-cyan-500/20">
                    <div class="text-xl sm:text-2xl font-bold text-cyan-400 mb-1">3D</div>
                    <div class="text-xs sm:text-sm text-gray-300">Modelado Avanzado</div>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Stats Cards -->
          <div class="space-y-4 sm:space-y-6 md:space-y-8" transition:name="mission-stats">

            <!-- Stat Card 1 -->
            <div class="group relative">
              <div class="relative border-2 border-[rgba(6,182,212,0.3)] rounded-2xl bg-gradient-to-br from-[rgba(6,182,212,0.15)] via-[rgba(14,165,233,0.1)] to-[rgba(6,182,212,0.05)] backdrop-blur-xl p-4 sm:p-6 md:p-8 transition-all duration-500 hover:border-[rgba(6,182,212,0.6)] hover:shadow-xl hover:shadow-cyan-500/20 hover:scale-105">
                <div class="absolute inset-0 bg-gradient-to-br from-white/5 to-transparent rounded-2xl"></div>
                <div class="relative z-10 flex items-center gap-3 sm:gap-4">
                  <div class="w-12 h-12 sm:w-16 sm:h-16 bg-cyan-600/20 rounded-full flex items-center justify-center flex-shrink-0">
                    <span class="text-xl sm:text-2xl">🚀</span>
                  </div>
                  <div class="min-w-0">
                    <div class="text-2xl sm:text-3xl font-bold text-cyan-400 mb-1">10,000+</div>
                    <div class="text-sm sm:text-base text-gray-300 leading-tight">Proyectos completados exitosamente</div>
                  </div>
                </div>
              </div>
            </div>

            <!-- Stat Card 2 -->
            <div class="group relative">
              <div class="relative border-2 border-[rgba(59,130,246,0.3)] rounded-2xl bg-gradient-to-br from-[rgba(59,130,246,0.15)] via-[rgba(14,165,233,0.1)] to-[rgba(59,130,246,0.05)] backdrop-blur-xl p-4 sm:p-6 md:p-8 transition-all duration-500 hover:border-[rgba(59,130,246,0.6)] hover:shadow-xl hover:shadow-blue-500/20 hover:scale-105">
                <div class="absolute inset-0 bg-gradient-to-br from-white/5 to-transparent rounded-2xl"></div>
                <div class="relative z-10 flex items-center gap-3 sm:gap-4">
                  <div class="w-12 h-12 sm:w-16 sm:h-16 bg-blue-600/20 rounded-full flex items-center justify-center flex-shrink-0">
                    <span class="text-xl sm:text-2xl">🌍</span>
                  </div>
                  <div class="min-w-0">
                    <div class="text-2xl sm:text-3xl font-bold text-blue-400 mb-1">50+</div>
                    <div class="text-sm sm:text-base text-gray-300 leading-tight">Países donde se utiliza HYDRA²¹</div>
                  </div>
                </div>
              </div>
            </div>

            <!-- Stat Card 3 -->
            <div class="group relative">
              <div class="relative border-2 border-[rgba(139,92,246,0.3)] rounded-2xl bg-gradient-to-br from-[rgba(139,92,246,0.15)] via-[rgba(59,130,246,0.1)] to-[rgba(139,92,246,0.05)] backdrop-blur-xl p-4 sm:p-6 md:p-8 transition-all duration-500 hover:border-[rgba(139,92,246,0.6)] hover:shadow-xl hover:shadow-purple-500/20 hover:scale-105">
                <div class="absolute inset-0 bg-gradient-to-br from-white/5 to-transparent rounded-2xl"></div>
                <div class="relative z-10 flex items-center gap-3 sm:gap-4">
                  <div class="w-12 h-12 sm:w-16 sm:h-16 bg-purple-600/20 rounded-full flex items-center justify-center flex-shrink-0">
                    <span class="text-xl sm:text-2xl">⚡</span>
                  </div>
                  <div class="min-w-0">
                    <div class="text-2xl sm:text-3xl font-bold text-purple-400 mb-1">99.9%</div>
                    <div class="text-sm sm:text-base text-gray-300 leading-tight">Tiempo de actividad garantizado</div>
                  </div>
                </div>
              </div>
            </div>

          </div>
        </div>
      </div>
    </div>
  </section>
  </ViewportAnimator>

  <!-- Values Section -->
  <ViewportAnimator animation="stagger" delay={150} duration={400}>
  <section class="py-20 sm:py-24 md:py-28 lg:py-32 bg-black">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-6xl mx-auto">

        <!-- Section Header -->
        <div class="text-center mb-16 sm:mb-20 md:mb-24">
          <h2 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight mb-6 sm:mb-8" transition:name="values-title">
            Nuestros Valores
          </h2>
          <p class="text-lg sm:text-xl md:text-2xl text-gray-300 leading-relaxed max-w-4xl mx-auto">
            Los principios que guían nuestro trabajo y definen nuestra cultura empresarial
          </p>
        </div>

        <!-- Values Grid -->
        <div class="grid grid-cols-1 sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 sm:gap-8 md:gap-10 lg:gap-12" transition:name="values-grid">

          <!-- Value Card 1: Precisión -->
          <div class="group relative">
            <div class="relative h-full border-2 border-[rgba(59,130,246,0.3)] rounded-3xl bg-gradient-to-br from-[rgba(59,130,246,0.15)] via-[rgba(14,165,233,0.1)] to-[rgba(59,130,246,0.05)] backdrop-blur-xl p-6 sm:p-8 md:p-10 text-center transition-all duration-500 hover:border-[rgba(59,130,246,0.6)] hover:shadow-2xl hover:shadow-blue-500/20 hover:scale-105">

              <!-- Glassmorphism overlay -->
              <div class="absolute inset-0 bg-gradient-to-br from-white/5 to-transparent rounded-3xl"></div>

              <div class="relative z-10 space-y-4 sm:space-y-6">
                <!-- Icon -->
                <div class="w-16 h-16 sm:w-20 sm:h-20 bg-blue-600/20 rounded-full flex items-center justify-center mx-auto mb-4 sm:mb-6 group-hover:scale-110 transition-transform duration-300">
                  <svg class="w-8 h-8 sm:w-10 sm:h-10 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                  </svg>
                </div>

                <!-- Title -->
                <h3 class="text-xl sm:text-2xl font-bold text-white mb-3 sm:mb-4">Precisión</h3>

                <!-- Description -->
                <p class="text-sm sm:text-base text-gray-300 leading-relaxed">
                  Cada cálculo, cada algoritmo, cada resultado debe ser exacto. La precisión no es negociable en la ingeniería.
                </p>

                <!-- Highlight -->
                <div class="mt-4 sm:mt-6 p-3 bg-blue-600/10 rounded-xl border border-blue-500/20">
                  <div class="text-xs sm:text-sm text-blue-400 font-semibold">99.99% Exactitud</div>
                </div>
              </div>
            </div>
          </div>

          <!-- Value Card 2: Innovación -->
          <div class="group relative">
            <div class="relative h-full border-2 border-[rgba(6,182,212,0.3)] rounded-3xl bg-gradient-to-br from-[rgba(6,182,212,0.15)] via-[rgba(14,165,233,0.1)] to-[rgba(6,182,212,0.05)] backdrop-blur-xl p-6 sm:p-8 md:p-10 text-center transition-all duration-500 hover:border-[rgba(6,182,212,0.6)] hover:shadow-2xl hover:shadow-cyan-500/20 hover:scale-105">

              <!-- Glassmorphism overlay -->
              <div class="absolute inset-0 bg-gradient-to-br from-white/5 to-transparent rounded-3xl"></div>

              <div class="relative z-10 space-y-4 sm:space-y-6">
                <!-- Icon -->
                <div class="w-16 h-16 sm:w-20 sm:h-20 bg-cyan-600/20 rounded-full flex items-center justify-center mx-auto mb-4 sm:mb-6 group-hover:scale-110 transition-transform duration-300">
                  <svg class="w-8 h-8 sm:w-10 sm:h-10 text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                  </svg>
                </div>

                <!-- Title -->
                <h3 class="text-xl sm:text-2xl font-bold text-white mb-3 sm:mb-4">Innovación</h3>

                <!-- Description -->
                <p class="text-sm sm:text-base text-gray-300 leading-relaxed">
                  Constantemente buscamos nuevas formas de mejorar, integrar tecnologías emergentes y superar límites.
                </p>

                <!-- Highlight -->
                <div class="mt-4 sm:mt-6 p-3 bg-cyan-600/10 rounded-xl border border-cyan-500/20">
                  <div class="text-xs sm:text-sm text-cyan-400 font-semibold">Tecnología de Vanguardia</div>
                </div>
              </div>
            </div>
          </div>

          <!-- Value Card 3: Colaboración -->
          <div class="group relative">
            <div class="relative h-full border-2 border-[rgba(139,92,246,0.3)] rounded-3xl bg-gradient-to-br from-[rgba(139,92,246,0.15)] via-[rgba(59,130,246,0.1)] to-[rgba(139,92,246,0.05)] backdrop-blur-xl p-6 sm:p-8 md:p-10 text-center transition-all duration-500 hover:border-[rgba(139,92,246,0.6)] hover:shadow-2xl hover:shadow-purple-500/20 hover:scale-105">

              <!-- Glassmorphism overlay -->
              <div class="absolute inset-0 bg-gradient-to-br from-white/5 to-transparent rounded-3xl"></div>

              <div class="relative z-10 space-y-4 sm:space-y-6">
                <!-- Icon -->
                <div class="w-16 h-16 sm:w-20 sm:h-20 bg-purple-600/20 rounded-full flex items-center justify-center mx-auto mb-4 sm:mb-6 group-hover:scale-110 transition-transform duration-300">
                  <svg class="w-8 h-8 sm:w-10 sm:h-10 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 515.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
                  </svg>
                </div>

                <!-- Title -->
                <h3 class="text-xl sm:text-2xl font-bold text-white mb-3 sm:mb-4">Colaboración</h3>

                <!-- Description -->
                <p class="text-sm sm:text-base text-gray-300 leading-relaxed">
                  Creemos en el poder del trabajo en equipo y en construir herramientas que fomenten la colaboración.
                </p>

                <!-- Highlight -->
                <div class="mt-4 sm:mt-6 p-3 bg-purple-600/10 rounded-xl border border-purple-500/20">
                  <div class="text-xs sm:text-sm text-purple-400 font-semibold">Trabajo en Equipo</div>
                </div>
              </div>
            </div>
          </div>

        </div>
      </div>
    </div>
  </section>
  </ViewportAnimator>

  <!-- CTA Section -->
  <ViewportAnimator animation="slide-up" delay={200} duration={400}>
  <section class="cta-section py-20 sm:py-24 md:py-28 lg:py-32 bg-black">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-5xl mx-auto">

        <!-- CTA Card -->
        <div class="group relative">
          <div class="relative border-2 border-[rgba(14,165,233,0.3)] rounded-3xl bg-gradient-to-br from-[rgba(14,165,233,0.15)] via-[rgba(59,130,246,0.1)] to-[rgba(14,165,233,0.05)] backdrop-blur-xl p-8 sm:p-10 md:p-12 lg:p-16 text-center transition-all duration-500 hover:border-[rgba(14,165,233,0.6)] hover:shadow-2xl hover:shadow-blue-500/20">

            <!-- Glassmorphism overlay -->
            <div class="absolute inset-0 bg-gradient-to-br from-white/5 to-transparent rounded-3xl"></div>

            <div class="relative z-10">
              <!-- Title -->
              <h2 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight mb-6 sm:mb-8" transition:name="cta-title">
                ¿Listo para experimentar <span class="gradient-text-blue-cyan">HYDRA²¹</span>?
              </h2>

              <!-- Description -->
              <p class="text-xl text-gray-300 leading-relaxed mb-10 max-w-3xl mx-auto" transition:name="cta-description">
                Únete a miles de ingenieros que ya están revolucionando su trabajo con nuestras herramientas de vanguardia.
              </p>

              <!-- Buttons -->
              <div class="btn-group">
                <a
                  href="/descargas"
                  class="btn-modern-primary"
                >
                  <!-- Windows Logo SVG -->
                  <svg class="icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M0 3.449L9.75 2.1v9.451H0m10.949-9.602L24 0v11.4H10.949M0 12.6h9.75v9.451L0 20.699M10.949 12.6H24V24l-13.051-1.351"/>
                  </svg>
                  Descargar HYDRA²¹
                </a>
                <a
                  href="/"
                  class="btn-modern-secondary"
                >
                  <!-- Home SVG -->
                  <svg class="icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                          d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
                  </svg>
                  Volver al Inicio
                </a>
              </div>

              <!-- Additional Info -->
              <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 mt-12 pt-8 border-t border-gray-700/50">
                <div class="text-center">
                  <div class="text-2xl font-bold text-blue-400 mb-1">24/7</div>
                  <div class="text-sm text-gray-400">Soporte Técnico</div>
                </div>
                <div class="text-center">
                  <div class="text-2xl font-bold text-cyan-400 mb-1">30 días</div>
                  <div class="text-sm text-gray-400">Prueba Gratuita</div>
                </div>
                <div class="text-center">
                  <div class="text-2xl font-bold text-purple-400 mb-1">∞</div>
                  <div class="text-sm text-gray-400">Actualizaciones</div>
                </div>
              </div>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>
  </ViewportAnimator>

  <Footer />
</Layout>

<style>
  /* About background image */
  .about-background {
    background-image: url('/assets/images/fondo_3_dark.png');
    background-size: cover;
    background-position: center center;
    background-repeat: no-repeat;
    opacity: 0.5;
    background-attachment: scroll;
  }

  /* Mobile specific background optimizations */
  @media (max-width: 767px) {
    .about-background {
      opacity: 0.5;
      background-size: cover;
      background-position: center center;
      background-attachment: scroll;
    }
  }

  @media (min-width: 768px) {
    .about-background {
      background-attachment: fixed;
      opacity: 0.5;
    }
  }

  /* Grid pattern */
  .grid-pattern {
    background-image:
      linear-gradient(rgba(59, 130, 246, 0.1) 1px, transparent 1px),
      linear-gradient(90deg, rgba(59, 130, 246, 0.1) 1px, transparent 1px);
    background-size: 50px 50px;
  }

  /* Enhanced gradient text */
  .gradient-text-blue-cyan {
    background: linear-gradient(135deg, #60a5fa 0%, #06b6d4 50%, #8b5cf6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    filter: drop-shadow(0 0 12px rgba(59, 130, 246, 0.4));
  }

  /* Floating animation following Hero patterns */
  @keyframes float-gentle {
    0%, 100% {
      transform: translateY(0px) rotate(0deg) scale(1);
    }
    25% {
      transform: translateY(-8px) rotate(0.5deg) scale(1.02);
    }
    50% {
      transform: translateY(-10px) rotate(1deg) scale(1.03);
    }
    75% {
      transform: translateY(-6px) rotate(0.5deg) scale(1.02);
    }
  }

  .animate-float-gentle {
    animation: float-gentle 8s ease-in-out infinite;
  }

  /* Mobile optimizations for animations */
  @media (max-width: 640px) {
    .animate-float-gentle {
      animation: float-gentle 12s ease-in-out infinite;
    }

    @keyframes float-gentle {
      0%, 100% {
        transform: translateY(0px) rotate(0deg) scale(1);
      }
      50% {
        transform: translateY(-5px) rotate(0.5deg) scale(1.01);
      }
    }
  }

  /* Performance optimizations for <400px */
  @media (max-width: 400px) {
    .animate-float-gentle {
      animation: float-gentle 15s ease-in-out infinite;
    }

    /* Simplify animations for better performance */
    @keyframes float-gentle {
      0%, 100% {
        transform: translateY(0px) scale(1);
      }
      50% {
        transform: translateY(-3px) scale(1.005);
      }
    }
  }

  /* ——————————————————————————————————————————————
     BUTTON GROUP ALIGNMENT - EXACT SAME AS HERO
  ————————————————————————————————————————————— */

  /* About page button group alignment - Hero section */
  .about-hero-section .btn-group {
    align-items: center !important;
    justify-content: center !important;
  }

  @media (min-width: 1024px) {
    .about-hero-section .btn-group {
      align-items: flex-start !important;
      justify-content: flex-start !important;
    }
  }

  /* CTA section button group - Centered always */
  .cta-section .btn-group {
    align-items: center !important;
    justify-content: center !important;
  }

  /* General button group fallback - About specific */
  .btn-group {
    display: flex !important;
    flex-direction: column !important;
    gap: 16px !important;
    align-items: center !important;
    justify-content: center !important;
  }

  @media (min-width: 768px) {
    .btn-group {
      flex-direction: row !important;
      gap: 24px !important;
    }
  }

  @media (min-width: 1024px) {
    .about-hero-section .btn-group {
      align-items: flex-start !important;
      justify-content: flex-start !important;
    }
  }

  /* Mobile specific adjustments for small screens */
  @media (max-width: 640px) {
    .btn-group {
      gap: 12px !important;
    }
  }

  @media (max-width: 400px) {
    .btn-group {
      gap: 10px !important;
    }

    .btn-group .btn-secondary {
      width: 100% !important;
      max-width: 280px !important;
    }
  }

  /* ——————————————————————————————————————————————
     GLOBAL BUTTON SYSTEM
     Note: Button styles are now managed globally in src/styles/buttons.css
  ————————————————————————————————————————————— */

  /* Glassmorphism card hover effects */
  .group:hover .group-hover\:scale-110 {
    transform: scale(1.1);
  }

  /* Enhanced responsive design following Hero patterns */

  /* Mobile optimizations for screens ≤640px */
  @media (max-width: 640px) {
    /* Mobile: Single column layout */
    .grid {
      grid-template-columns: 1fr !important;
      gap: 1.5rem !important;
    }

    /* Reduce padding on mobile */
    .p-8 {
      padding: 1.5rem !important;
    }

    .p-12, .sm\:p-10, .md\:p-12, .lg\:p-16 {
      padding: 1.5rem !important;
    }

    /* Reduce section spacing on mobile */
    .py-20, .sm\:py-24, .md\:py-28, .lg\:py-32 {
      padding-top: 3rem !important;
      padding-bottom: 3rem !important;
    }

    /* Reduce margin between sections */
    .mb-16, .sm\:mb-20, .md\:mb-24 {
      margin-bottom: 2rem !important;
    }

    .mb-6, .sm\:mb-8 {
      margin-bottom: 1rem !important;
    }

    /* Adjust hero section for mobile */
    .min-h-screen {
      min-height: 100vh;
      padding-top: 2rem;
      padding-bottom: 2rem;
    }
  }

  /* Enhanced optimizations for screens ≤499px following Hero pattern */
  @media (max-width: 499px) {
    /* Background adjustments for better text contrast */
    .about-background {
      background-attachment: scroll;
      background-position: center center;
      background-size: cover;
      filter: brightness(1.2) contrast(1.2) saturate(1.1);
      opacity: 0.5;
    }

    /* Hero section takes full viewport */
    .relative.min-h-screen {
      min-height: 100vh;
      min-height: 100svh;
    }

    /* Enhanced text shadows for better readability */
    h1 {
      text-shadow: 0 3px 12px rgba(0, 0, 0, 0.9);
    }

    p {
      text-shadow: 0 2px 8px rgba(0, 0, 0, 0.8);
    }
  }

  /* Specific optimizations for screens <400px following Hero pattern */
  @media (max-width: 400px) {
    /* Hero section takes full viewport */
    .relative.min-h-screen {
      min-height: 100vh !important;
      min-height: 100svh !important;
    }

    /* Background adjustments similar to Hero */
    .about-background {
      opacity: 0.8 !important;
      filter: brightness(1.1) contrast(1.1) !important;
    }

    /* TITLE h1: sizes similar to Hero */
    h1 {
      font-size: 1.5rem !important;      /* 24px - same as Hero */
      line-height: 2rem !important;      /* 32px - same as Hero */
      margin-bottom: 0.5rem !important;
    }

    /* SUBTITLE p: sizes similar to Hero */
    p {
      font-size: 0.875rem !important;    /* 14px - same as Hero */
      line-height: 1.25rem !important;   /* 20px - same as Hero */
      margin-bottom: 0.75rem !important;
    }

    /* Section titles similar to Hero */
    .text-3xl, .sm\:text-4xl, .md\:text-5xl, .lg\:text-6xl {
      font-size: 1.5rem !important;      /* 24px - same as Hero */
      line-height: 2rem !important;      /* 32px - same as Hero */
    }

    /* Section descriptions similar to Hero */
    .text-lg, .sm\:text-xl, .md\:text-2xl {
      font-size: 0.875rem !important;    /* 14px - same as Hero */
      line-height: 1.25rem !important;   /* 20px - same as Hero */
    }

    /* Compact badge similar to Hero */
    .inline-flex.items-center {
      padding: 0.2rem 0.4rem !important;  /* 4px / 8px */
      font-size: 0.625rem !important;     /* 10px */
    }

    .inline-flex.items-center span {
      font-size: 0.75rem !important;      /* 12px */
    }

    /* CTA buttons similar to Hero */
    .flex.flex-col.md\:flex-row {
      gap: 0.75rem !important;           /* 12px - more space */
    }

    .flex.flex-col.md\:flex-row a {
      width: 100% !important;
      padding: 0.75rem 1.5rem !important; /* 12px top/bottom, 24px left/right */
      font-size: 1rem !important;         /* 16px - same as Hero */
      border-radius: 0.5rem !important;   /* 8px radius */
    }
  }

  /* Tablet optimizations (500-767px) - intermediate adjustments */
  @media (min-width: 500px) and (max-width: 767px) {
    .about-background {
      background-attachment: scroll;
      background-position: center center;
      background-size: cover;
      filter: brightness(1.1) contrast(1.1);
    }

    .relative.min-h-screen {
      min-height: 100svh;
    }

    /* Small tablet adjustments */
    .py-20, .sm\:py-24, .md\:py-28, .lg\:py-32 {
      padding-top: 4rem !important;
      padding-bottom: 4rem !important;
    }

    .mb-16, .sm\:mb-20, .md\:mb-24 {
      margin-bottom: 3rem !important;
    }
  }

  /* Tablet (≥768px) - background fixed and more transparent */
  @media (min-width: 768px) {
    .about-background {
      background-attachment: fixed;
      opacity: 0.4;
    }
  }

  @media (min-width: 768px) and (max-width: 1023px) {
    /* Tablet: 2 columns for values grid */
    .grid.md\:grid-cols-3 {
      grid-template-columns: repeat(2, 1fr) !important;
      gap: 2rem !important;
    }

    /* Center the third card on tablet */
    .grid.md\:grid-cols-3 > div:nth-child(3) {
      grid-column: 1 / -1;
      max-width: 60%;
      margin: 0 auto;
    }

    /* Tablet section spacing */
    .py-20, .sm\:py-24, .md\:py-28, .lg\:py-32 {
      padding-top: 5rem !important;
      padding-bottom: 5rem !important;
    }

    .mb-16, .sm\:mb-20, .md\:mb-24 {
      margin-bottom: 4rem !important;
    }
  }

  @media (min-width: 1024px) {
    /* Desktop: Optimal spacing */
    .py-20, .sm\:py-24, .md\:py-28, .lg\:py-32 {
      padding-top: 6rem !important;
      padding-bottom: 6rem !important;
    }

    .mb-16, .sm\:mb-20, .md\:mb-24 {
      margin-bottom: 5rem !important;
    }

    /* Better grid gaps on desktop */
    .grid {
      gap: 2.5rem !important;
    }

    .gap-12, .lg\:gap-16 {
      gap: 3rem !important;
    }
  }

  /* Force consistent black backgrounds across all sections */
  section {
    background-color: #000000 !important;
    background: #000000 !important;
  }

  /* Force footer black background */
  footer {
    background-color: #000000 !important;
    background: #000000 !important;
  }

  /* Ensure hero section maintains black background */
  .hero-section,
  .relative.min-h-screen {
    background-color: #000000 !important;
  }

  /* Ensure all page sections have black background */
  .bg-black {
    background-color: #000000 !important;
  }

  /* Override any potential background overrides */
  body {
    background-color: #000000 !important;
  }

  /* Ensure main layout has black background */
  main {
    background-color: #000000 !important;
  }

  /* Specific section background overrides */
  .relative.min-h-screen.flex.items-center.overflow-hidden.bg-black,
  section.bg-black,
  section#mision,
  .py-20.sm\\:py-24.md\\:py-28.lg\\:py-32.bg-black {
    background-color: #000000 !important;
    background: #000000 !important;
  }

  /* Ensure Layout component has black background */
  html,
  body,
  #__astro {
    background-color: #000000 !important;
    background: #000000 !important;
  }

  /* Override any potential Layout.astro background */
  .layout,
  .page-wrapper,
  .main-content {
    background-color: #000000 !important;
    background: #000000 !important;
  }

  /* Override Layout.astro theme backgrounds and decorative elements */
  .min-h-screen.relative.min-w-\\[300px\\] {
    background-color: #000000 !important;
    background: #000000 !important;
  }

  /* Hide or override Layout decorative elements */
  .absolute.inset-0.diagonal-grid,
  .absolute.top-10.left-10,
  .absolute.bottom-10.right-10,
  .absolute.top-1\\/2.left-1\\/2,
  .absolute.top-1\\/4.right-1\\/4 {
    display: none !important;
  }

  /* Override theme-based CSS variables for this page */
  :root {
    --color-background: #000000 !important;
    --color-foreground: #ffffff !important;
  }

  /* Force dark theme styles */
  html.light,
  html.dark {
    background-color: #000000 !important;
  }

  /* Override any theme-bg classes */
  .theme-bg {
    background-color: #000000 !important;
    background: #000000 !important;
  }

  /* Enhanced card animations */
  .group {
    transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .group:hover {
    transform: translateY(-8px);
  }

  /* Particle effect for enhanced visual appeal */
  @keyframes particle-float {
    0%, 100% {
      transform: translateY(0px) translateX(0px) rotate(0deg);
      opacity: 0.3;
    }
    50% {
      transform: translateY(-20px) translateX(10px) rotate(180deg);
      opacity: 0.7;
    }
  }

  /* Add subtle particle effects to cards */
  .group::before {
    content: '';
    position: absolute;
    top: 10%;
    right: 10%;
    width: 4px;
    height: 4px;
    background: rgba(59, 130, 246, 0.6);
    border-radius: 50%;
    animation: particle-float 8s ease-in-out infinite;
    z-index: 1;
  }

  .group::after {
    content: '';
    position: absolute;
    bottom: 15%;
    left: 15%;
    width: 3px;
    height: 3px;
    background: rgba(6, 182, 212, 0.5);
    border-radius: 50%;
    animation: particle-float 6s ease-in-out infinite reverse;
    z-index: 1;
  }

  /* ——————————————————————————————————————————————
     MODERN BUTTON SYSTEM - HERO STYLE REPLICATION
     Note: Modern buttons matching the Hero component design
  ————————————————————————————————————————————— */

  /* Modern Primary Button - Main CTA */
  .btn-modern-primary {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;

    /* Adaptive padding for optimal touch targets */
    padding: 16px 32px;
    min-width: 180px;

    /* Typography */
    font-size: 16px;
    font-weight: 600;
    line-height: 1.2;
    text-decoration: none;
    white-space: nowrap;

    /* Modern visual styling */
    color: #ffffff;
    background: linear-gradient(135deg, #1e40af 0%, #1e3a8a 100%);
    border: 2px solid rgba(59, 130, 246, 0.4);
    border-radius: 12px;
    box-shadow:
      0 4px 15px rgba(59, 130, 246, 0.25),
      0 0 0 1px rgba(255, 255, 255, 0.1) inset;

    /* Interaction */
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);

    /* Prevent text selection */
    user-select: none;
    -webkit-user-select: none;

    /* Glassmorphism effect */
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }

  /* Modern Secondary Button - Secondary CTA */
  .btn-modern-secondary {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;

    /* Adaptive padding for optimal touch targets */
    padding: 16px 32px;
    min-width: 180px;

    /* Typography */
    font-size: 16px;
    font-weight: 500;
    line-height: 1.2;
    text-decoration: none;
    white-space: nowrap;

    /* Modern visual styling */
    color: #e0f2fe;
    background: rgba(15, 23, 42, 0.8);
    border: 2px solid rgba(59, 130, 246, 0.3);
    border-radius: 12px;
    box-shadow:
      0 4px 15px rgba(0, 0, 0, 0.2),
      0 0 0 1px rgba(255, 255, 255, 0.05) inset;

    /* Interaction */
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);

    /* Prevent text selection */
    user-select: none;
    -webkit-user-select: none;

    /* Glassmorphism effect */
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }

  /* Button icon styling */
  .btn-modern-primary .icon,
  .btn-modern-secondary .icon,
  .btn-modern-primary svg,
  .btn-modern-secondary svg {
    width: 20px;
    height: 20px;
    fill: currentColor;
    stroke: currentColor;
    flex-shrink: 0;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Primary Button Hover States */
  .btn-modern-primary:hover {
    transform: translateY(-2px) scale(1.02);
    background: linear-gradient(135deg, #1d4ed8 0%, #1e40af 100%);
    border-color: rgba(59, 130, 246, 0.6);
    box-shadow:
      0 8px 25px rgba(59, 130, 246, 0.4),
      0 0 0 1px rgba(255, 255, 255, 0.2) inset;
    color: #ffffff;
  }

  .btn-modern-primary:hover .icon,
  .btn-modern-primary:hover svg {
    transform: scale(1.1);
  }

  /* Secondary Button Hover States */
  .btn-modern-secondary:hover {
    transform: translateY(-2px) scale(1.02);
    background: rgba(59, 130, 246, 0.15);
    border-color: rgba(59, 130, 246, 0.5);
    box-shadow:
      0 8px 25px rgba(59, 130, 246, 0.3),
      0 0 0 1px rgba(255, 255, 255, 0.1) inset;
    color: #ffffff;
  }

  .btn-modern-secondary:hover .icon,
  .btn-modern-secondary:hover svg {
    transform: scale(1.1);
  }

  /* Button Active States */
  .btn-modern-primary:active,
  .btn-modern-secondary:active {
    transform: translateY(0px) scale(1);
    transition-duration: 0.1s;
  }

  /* Button Focus States for Accessibility */
  .btn-modern-primary:focus,
  .btn-modern-secondary:focus {
    outline: none;
    box-shadow:
      0 0 0 3px rgba(59, 130, 246, 0.5),
      0 4px 15px rgba(59, 130, 246, 0.25);
  }

  /* Responsive Design */
  @media (max-width: 640px) {
    .btn-modern-primary,
    .btn-modern-secondary {
      padding: 14px 24px;
      min-width: 160px;
      font-size: 15px;
      gap: 6px;
    }

    .btn-modern-primary .icon,
    .btn-modern-secondary .icon,
    .btn-modern-primary svg,
    .btn-modern-secondary svg {
      width: 18px;
      height: 18px;
    }
  }

  @media (max-width: 400px) {
    .btn-modern-primary,
    .btn-modern-secondary {
      padding: 12px 20px;
      min-width: 140px;
      font-size: 14px;
      gap: 4px;
    }

    .btn-modern-primary .icon,
    .btn-modern-secondary .icon,
    .btn-modern-primary svg,
    .btn-modern-secondary svg {
      width: 16px;
      height: 16px;
    }
  }

  @media (min-width: 1024px) {
    .btn-modern-primary,
    .btn-modern-secondary {
      padding: 18px 36px;
      min-width: 200px;
      font-size: 17px;
    }
  }

  /* Accessibility - Respect user's motion preferences */
  @media (prefers-reduced-motion: reduce) {
    .btn-modern-primary,
    .btn-modern-secondary,
    .btn-modern-primary svg,
    .btn-modern-secondary svg,
    .btn-modern-primary .icon,
    .btn-modern-secondary .icon {
      transition: none;
      animation: none;
    }

    .btn-modern-primary:hover,
    .btn-modern-secondary:hover {
      transform: none;
    }
  }

  /* High contrast mode support */
  @media (prefers-contrast: high) {
    .btn-modern-primary,
    .btn-modern-secondary {
      border: 2px solid #ffffff;
      background-color: transparent;
      color: #ffffff;
    }
  }
</style>