---
// Features Section with Brain Hub Design for HYDRA²¹
---

<section id="caracteristicas" class="features-section relative min-h-screen flex items-center overflow-hidden">
  <!-- Background Image - Matching hero style -->
  <div class="absolute inset-0 bg-black features-background">
    <div class="absolute inset-0 bg-black/40 xs:bg-black/30 sm:bg-black/25 md:bg-black/20 lg:bg-black/30"></div>
  </div>
  
  <!-- Top blur transition from hero -->
  <div class="absolute top-0 left-0 right-0 h-20 sm:h-24 md:h-32 lg:h-40 bg-gradient-to-b from-black via-black/80 to-transparent z-20"></div>
  <div class="absolute top-0 left-0 right-0 h-16 sm:h-20 md:h-24 lg:h-32 bg-gradient-to-b from-black/90 to-transparent z-21"></div>
  <div class="absolute top-0 left-0 right-0 h-12 sm:h-14 md:h-16 lg:h-20 bg-gradient-to-b from-black to-transparent z-22"></div>
  
  <!-- Bottom blur transition to next section -->
  <div class="absolute bottom-0 left-0 right-0 h-20 sm:h-24 md:h-32 lg:h-40 bg-gradient-to-t from-black via-black/80 to-transparent z-20"></div>
  <div class="absolute bottom-0 left-0 right-0 h-16 sm:h-20 md:h-24 lg:h-32 bg-gradient-to-t from-black/90 to-transparent z-21"></div>
  <div class="absolute bottom-0 left-0 right-0 h-12 sm:h-14 md:h-16 lg:h-20 bg-gradient-to-t from-black to-transparent z-22"></div>
  
  <!-- Geometric grid pattern -->
  <div class="absolute inset-0 opacity-4 xs:opacity-5 sm:opacity-6 md:opacity-8 lg:opacity-10">
    <div class="absolute inset-0 grid-pattern"></div>
  </div>

  <!-- Main content container -->
  <div class="container mx-auto px-4 xs:px-6 sm:px-8 md:px-10 lg:px-12 xl:px-16 relative z-10 py-8 xs:py-10 sm:py-12 md:py-16 lg:py-20 xl:py-24">
    <!-- Section Title -->
    <div class="text-center mb-12 sm:mb-16 md:mb-20 lg:mb-24">
      <div class="inline-flex items-center gap-1 sm:gap-2 bg-gray-900/90 backdrop-blur-sm border border-blue-500/30 rounded-full px-2 py-1 text-xs sm:text-sm mb-4 sm:mb-6">
        <span class="text-orange-400 text-xs sm:text-sm">🧠</span>
        <span class="text-blue-400 font-medium text-xs sm:text-xs">#INTELIGENCIA HIDRÁULICA</span>
      </div>

      <!-- Title with Brain Image -->
      <div class="flex flex-col items-center justify-center mb-6 sm:mb-8 md:mb-10 lg:mb-12">
        <!-- Main Title -->
        <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-white leading-tight text-center mb-6 sm:mb-8 md:mb-10">
          Conectando
          <span class="block text-blue-400">Ideas con</span>
          <span class="block text-blue-300">Soluciones</span>
        </h2>

        <!-- Brain Image - MASSIVE and Prominent - OPTIMIZED -->
        <div class="flex justify-center mb-8 sm:mb-10 md:mb-12">
          <div class="relative">
            <img
              src="/assets/images/brain_3D.png"
              alt="Brain"
              class="w-48 h-48 xs:w-56 xs:h-56 sm:w-64 sm:h-64 md:w-80 md:h-80 lg:w-96 lg:h-96 xl:w-[28rem] xl:h-[28rem] 2xl:w-[32rem] 2xl:h-[32rem] opacity-95 filter drop-shadow-2xl animate-float"
              loading="lazy"
              decoding="async"
            />
          </div>
        </div>
      </div>

      <p class="text-lg sm:text-xl text-gray-300 leading-relaxed max-w-2xl mx-auto">
        Donde cada conexión neuronal representa<br class="hidden sm:block"/>
        una capacidad avanzada de ingeniería.
      </p>
    </div>

    <!-- Modern Grid Container -->
    <div class="relative flex justify-center items-center">
      <div class="features-grid-container relative w-full max-w-7xl mx-auto">

        <!-- Features Grid - Improved Responsive Layout -->
        <div class="grid grid-cols-1 sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 sm:gap-10 md:gap-8 lg:gap-10 xl:gap-12">

          <!-- Hydraulic Analysis -->
          <div class="group relative h-64 w-full border-2 border-[rgba(59,130,246,0.5)] rounded-3xl bg-gradient-to-br from-[rgba(59,130,246,1)] to-[rgba(59,130,246,0.01)] text-white p-6 flex justify-center items-left flex-col gap-3 backdrop-blur-xl cursor-pointer transition-all duration-300 hover:translate-y-[-0.125rem]">
            <!-- Background Image (Hidden by default, shown on hover) -->
            <div class="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-700 z-0 rounded-3xl overflow-hidden">
              <img src="/assets/images/features/hydraulic-analysis.jpg" alt="Hydraulic Analysis" class="w-full h-full object-cover filter group-hover:brightness-110 group-hover:contrast-110" loading="lazy" decoding="async" />
              <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-black/20"></div>
            </div>

            <!-- Content -->
            <div class="relative z-10">
              <h3 class="text-2xl font-bold mb-2">Análisis Hidráulico</h3>
              <p class="text-sm opacity-90 leading-relaxed">
                Análisis completo de sistemas hidráulicos con precisión avanzada y simulación en tiempo real
              </p>
            </div>
          </div>

          <!-- Pipe Network Design -->
          <div class="group relative h-64 w-full border-2 border-[rgba(14,165,233,0.5)] rounded-3xl bg-gradient-to-br from-[rgba(14,165,233,1)] to-[rgba(14,165,233,0.01)] text-white p-6 flex justify-center items-left flex-col gap-3 backdrop-blur-xl cursor-pointer transition-all duration-300 hover:translate-y-[-0.125rem]">
            <!-- Background Image (Hidden by default, shown on hover) -->
            <div class="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-700 z-0 rounded-3xl overflow-hidden">
              <img src="/assets/images/features/design.jpg" alt="Pipe Network Design" class="w-full h-full object-cover filter group-hover:brightness-110 group-hover:contrast-110" />
              <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-black/20"></div>
            </div>

            <!-- Content -->
            <div class="relative z-10">
              <h3 class="text-2xl font-bold mb-2">Diseño de Redes</h3>
              <p class="text-sm opacity-90 leading-relaxed">
                Diseño inteligente de redes de tuberías y sistemas de distribución optimizados
              </p>
            </div>
          </div>

          <!-- Flow Calculations -->
          <div class="group relative h-64 w-full border-2 border-[rgba(99,102,241,0.5)] rounded-3xl bg-gradient-to-br from-[rgba(99,102,241,1)] to-[rgba(99,102,241,0.01)] text-white p-6 flex justify-center items-left flex-col gap-3 backdrop-blur-xl cursor-pointer transition-all duration-300 hover:translate-y-[-0.125rem]">
            <!-- Background Image (Hidden by default, shown on hover) -->
            <div class="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-700 z-0 rounded-3xl overflow-hidden">
              <img src="/assets/images/features/calculations.jpg" alt="Flow Calculations" class="w-full h-full object-cover filter group-hover:brightness-110 group-hover:contrast-110" />
              <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-black/20"></div>
            </div>

            <!-- Content -->
            <div class="relative z-10">
              <h3 class="text-2xl font-bold mb-2">Cálculos de Flujo</h3>
              <p class="text-sm opacity-90 leading-relaxed">
                Cálculos precisos de flujo, velocidad y pérdidas de carga con algoritmos avanzados
              </p>
            </div>
          </div>

          <!-- Pressure Analysis -->
          <div class="group relative h-64 w-full border-2 border-[rgba(100,116,139,0.5)] rounded-3xl bg-gradient-to-br from-[rgba(100,116,139,1)] to-[rgba(100,116,139,0.01)] text-white p-6 flex justify-center items-left flex-col gap-3 backdrop-blur-xl cursor-pointer transition-all duration-300 hover:translate-y-[-0.125rem]">
            <!-- Background Image (Hidden by default, shown on hover) -->
            <div class="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-700 z-0 rounded-3xl overflow-hidden">
              <img src="/assets/images/features/pressure-analysis.jpg" alt="Pressure Analysis" class="w-full h-full object-cover filter group-hover:brightness-110 group-hover:contrast-110" />
              <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-black/20"></div>
            </div>

            <!-- Content -->
            <div class="relative z-10">
              <h3 class="text-2xl font-bold mb-2">Análisis de Presión</h3>
              <p class="text-sm opacity-90 leading-relaxed">
                Análisis detallado de presiones y comportamiento dinámico del sistema
              </p>
            </div>
          </div>

          <!-- System Optimization -->
          <div class="group relative h-64 w-full border-2 border-[rgba(79,70,229,0.5)] rounded-3xl bg-gradient-to-br from-[rgba(79,70,229,1)] to-[rgba(79,70,229,0.01)] text-white p-6 flex justify-center items-left flex-col gap-3 backdrop-blur-xl cursor-pointer transition-all duration-300 hover:translate-y-[-0.125rem]">
            <!-- Background Image (Hidden by default, shown on hover) -->
            <div class="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-700 z-0 rounded-3xl overflow-hidden">
              <img src="/assets/images/features/optimization.jpg" alt="System Optimization" class="w-full h-full object-cover filter group-hover:brightness-110 group-hover:contrast-110" />
              <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-black/20"></div>
            </div>

            <!-- Content -->
            <div class="relative z-10">
              <h3 class="text-2xl font-bold mb-2">Optimización de Sistemas</h3>
              <p class="text-sm opacity-90 leading-relaxed">
                Optimización automática para máxima eficiencia energética y rendimiento
              </p>
            </div>
          </div>

          <!-- 3D Modeling -->
          <div class="group relative h-64 w-full border-2 border-[rgba(6,182,212,0.5)] rounded-3xl bg-gradient-to-br from-[rgba(6,182,212,1)] to-[rgba(6,182,212,0.01)] text-white p-6 flex justify-center items-left flex-col gap-3 backdrop-blur-xl cursor-pointer transition-all duration-300 hover:translate-y-[-0.125rem]">
            <!-- Background Image (Hidden by default, shown on hover) -->
            <div class="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-700 z-0 rounded-3xl overflow-hidden">
              <img src="/assets/images/features/modeling.jpg" alt="3D Modeling" class="w-full h-full object-cover filter group-hover:brightness-110 group-hover:contrast-110" />
              <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-black/20"></div>
            </div>

            <!-- Content -->
            <div class="relative z-10">
              <h3 class="text-2xl font-bold mb-2">Modelado 3D</h3>
              <p class="text-sm opacity-90 leading-relaxed">
                Modelado tridimensional avanzado para visualización completa y análisis detallado
              </p>
            </div>
          </div>

        </div> <!-- End of grid -->
      </div> <!-- End of features-grid-container -->

      </div>
    </div>
  </div>
</section>




<style>
  .features-section {
    background: #000000;
  }

  /* Features background image - Matching hero style */
  .features-background {
    background-image: url('/assets/images/fondo_2.jpeg');
    background-size: cover;
    background-position: center center;
    background-repeat: no-repeat;
    opacity: 0.3;
    background-attachment: scroll;
  }

  /* Mobile specific background optimizations */
  @media (max-width: 767px) {
    .features-background {
      opacity: 0.6;
      background-size: cover;
      background-position: center center;
      background-attachment: scroll;
    }
  }

  @media (min-width: 768px) {
    .features-background {
      background-attachment: fixed;
      opacity: 0.4;
    }
  }

  /* Grid pattern - Matching hero style */
  .grid-pattern {
    display: none;
  }

  /* Modern Grid Layout Styles */
  .features-grid-container {
    padding: 2rem 0;
  }

  .feature-card {
    transition: all 0.7s cubic-bezier(0.4, 0, 0.2, 1);
    backdrop-filter: blur(20px);
    border-radius: 1.5rem;
    overflow: hidden;
    position: relative;
    background: rgba(0, 0, 0, 0.2);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .feature-card:hover {
    transform: translateY(-12px) scale(1.02);
    box-shadow:
      0 35px 80px -15px rgba(0, 0, 0, 0.7),
      0 0 0 1px rgba(255, 255, 255, 0.2),
      inset 0 1px 0 rgba(255, 255, 255, 0.1);
  }

  /* Staggered animation for cards */
  .feature-card:nth-child(2) { animation-delay: 0.1s; }
  .feature-card:nth-child(3) { animation-delay: 0.2s; }
  .feature-card:nth-child(4) { animation-delay: 0.3s; }
  .feature-card:nth-child(5) { animation-delay: 0.4s; }
  .feature-card:nth-child(6) { animation-delay: 0.5s; }
  .feature-card:nth-child(7) { animation-delay: 0.6s; }

  /* Particle animations */
  .particle {
    animation-duration: 8s;
    animation-iteration-count: infinite;
    animation-timing-function: ease-in-out;
  }

  .particle-1 { top: 20%; left: 15%; animation-name: float-particle-1; animation-delay: 0s; }
  .particle-2 { top: 60%; left: 80%; animation-name: float-particle-2; animation-delay: -2s; }
  .particle-3 { top: 80%; left: 30%; animation-name: float-particle-3; animation-delay: -4s; }
  .particle-4 { top: 25%; left: 70%; animation-name: float-particle-1; animation-delay: -1s; }
  .particle-5 { top: 70%; left: 20%; animation-name: float-particle-2; animation-delay: -3s; }
  .particle-6 { top: 40%; left: 85%; animation-name: float-particle-3; animation-delay: -5s; }
  .particle-7 { top: 15%; left: 60%; animation-name: float-particle-1; animation-delay: -1.5s; }
  .particle-8 { top: 75%; left: 10%; animation-name: float-particle-2; animation-delay: -3.5s; }
  .particle-9 { top: 50%; left: 90%; animation-name: float-particle-3; animation-delay: -2.5s; }
  .particle-10 { top: 30%; left: 25%; animation-name: float-particle-1; animation-delay: -0.5s; }
  .particle-11 { top: 65%; left: 75%; animation-name: float-particle-2; animation-delay: -4.5s; }
  .particle-12 { top: 85%; left: 50%; animation-name: float-particle-3; animation-delay: -1.8s; }
  .particle-13 { top: 10%; left: 40%; animation-name: float-particle-1; animation-delay: -2.8s; }
  .particle-14 { top: 55%; left: 65%; animation-name: float-particle-2; animation-delay: -0.8s; }
  .particle-15 { top: 90%; left: 20%; animation-name: float-particle-3; animation-delay: -3.8s; }
  .particle-16 { top: 35%; left: 80%; animation-name: float-particle-1; animation-delay: -1.2s; }
  .particle-17 { top: 20%; left: 50%; animation-name: float-particle-2; animation-delay: -4.2s; }
  .particle-18 { top: 75%; left: 35%; animation-name: float-particle-3; animation-delay: -2.2s; }

  @keyframes float-particle-1 {
    0%, 100% {
      transform: translate(0, 0) scale(1);
      opacity: 0.6;
    }
    25% {
      transform: translate(10px, -15px) scale(1.2);
      opacity: 0.8;
    }
    50% {
      transform: translate(-5px, -25px) scale(0.8);
      opacity: 1;
    }
    75% {
      transform: translate(-15px, -10px) scale(1.1);
      opacity: 0.7;
    }
  }

  @keyframes float-particle-2 {
    0%, 100% {
      transform: translate(0, 0) scale(1);
      opacity: 0.4;
    }
    33% {
      transform: translate(-12px, -20px) scale(1.3);
      opacity: 0.9;
    }
    66% {
      transform: translate(8px, -30px) scale(0.9);
      opacity: 0.6;
    }
  }

  @keyframes float-particle-3 {
    0%, 100% {
      transform: translate(0, 0) scale(1);
      opacity: 0.5;
    }
    50% {
      transform: translate(15px, -35px) scale(1.4);
      opacity: 0.8;
    }
  }

  /* Additional blur layer for smoother transitions */
  .features-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 80px;
    background: linear-gradient(
      to bottom,
      rgba(0, 0, 0, 0.9) 0%,
      rgba(0, 0, 0, 0.7) 30%,
      rgba(0, 0, 0, 0.4) 60%,
      transparent 100%
    );
    backdrop-filter: blur(2px);
    z-index: 24;
    pointer-events: none;
  }

  .features-section::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 80px;
    background: linear-gradient(
      to top,
      rgba(0, 0, 0, 0.9) 0%,
      rgba(0, 0, 0, 0.7) 30%,
      rgba(0, 0, 0, 0.4) 60%,
      transparent 100%
    );
    backdrop-filter: blur(2px);
    z-index: 24;
    pointer-events: none;
  }

  /* Enhanced glassmorphism effects */
  .feature-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
    opacity: 0;
    transition: opacity 0.5s ease;
  }

  .feature-card:hover::before {
    opacity: 1;
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .features-section::before,
    .features-section::after {
      height: 60px;
      backdrop-filter: blur(1px);
    }

    .features-grid-container {
      padding: 1rem 0;
    }

    .feature-card {
      min-h: 280px !important;
    }

    .feature-card h3 {
      font-size: 1.25rem !important;     /* 20px - similar al Hero */
    }

    .feature-card p {
      font-size: 0.75rem !important;     /* 12px - similar al Hero */
    }

    /* Reduce particle effects on tablets and mobile */
    .particle {
      animation-duration: 12s;
      opacity: 0.4 !important;
    }

    /* Simplify hover effects on touch devices */
    .feature-card:hover {
      transform: translateY(-6px) scale(1.01);
    }
  }

  @media (max-width: 640px) {
    .feature-card {
      min-h: 260px !important;
      padding: 1.5rem !important;
    }

    .feature-card h3 {
      font-size: 1rem !important;        /* 16px - similar al Hero */
    }

    .feature-card p {
      font-size: 0.75rem !important;     /* 12px - similar al Hero */
    }

    /* Further reduce particle effects on mobile */
    .particle {
      opacity: 0.2 !important;
    }

    /* Minimal hover effects on mobile */
    .feature-card:hover {
      transform: translateY(-2px) scale(1.005);
    }
  }

  /* Enhanced responsive grid behavior */
  @media (min-width: 768px) and (max-width: 1023px) {
    /* Tablet: 2 columns */
    .grid {
      grid-template-columns: repeat(2, 1fr) !important;
    }
  }

  @media (max-width: 767px) {
    /* Mobile: 1 column */
    .grid {
      grid-template-columns: 1fr !important;
    }
  }

  @media (min-width: 1024px) {
    .features-section::before,
    .features-section::after {
      height: 100px;
      backdrop-filter: blur(3px);
    }
  }

  /* Enhanced mobile optimizations */
  @media (max-width: 480px) {
    .features-background {
      opacity: 0.8 !important;
      filter: brightness(1.2) contrast(1.2) saturate(1.1) !important;
    }

    .features-grid-container {
      padding: 0.5rem 0;
    }

    .feature-card {
      min-h: 220px !important;
      margin-bottom: 1rem;
    }

    .grid-pattern {
      background-size: 12px 12px !important;
      background-image: radial-gradient(circle at 1px 1px, rgba(59, 130, 246, 0.3) 1px, transparent 0) !important;
    }

    /* Enhanced text contrast */
    .features-section h2 {
      text-shadow: 0 4px 16px rgba(0, 0, 0, 1) !important;
    }

    .features-section p {
      text-shadow: 0 3px 12px rgba(0, 0, 0, 0.9) !important;
    }
  }

  @media (max-width: 400px) {
    /* Fondo similar al Hero */
    .features-background {
      opacity: 0.8 !important;
      filter: brightness(1.1) contrast(1.1) !important;
    }

    /* Cards más compactas */
    .feature-card {
      min-h: 200px !important;
    }

    /* Títulos y textos similares al Hero */
    .features-section h2 {
      font-size: 1.5rem !important;      /* 24px - igual que Hero */
      line-height: 2rem !important;      /* 32px - igual que Hero */
    }

    .features-section p {
      font-size: 0.875rem !important;    /* 14px - igual que Hero */
      line-height: 1.25rem !important;   /* 20px - igual que Hero */
    }

    /* Cards h3 y p */
    .feature-card h3 {
      font-size: 1rem !important;        /* 16px */
    }

    .feature-card p {
      font-size: 0.75rem !important;     /* 12px */
    }

    /* Badge similar al Hero */
    .features-section .inline-flex.items-center {
      padding: 0.2rem 0.4rem !important;  /* 4px / 8px */
      font-size: 0.625rem !important;     /* 10px */
    }
    .features-section .inline-flex.items-center span {
      font-size: 0.75rem !important;      /* 12px */
    }
  }

  /* Animación de flotación para el cerebro */
  @keyframes float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  .animate-float {
    animation: float 6s ease-in-out infinite;
  }
</style>
