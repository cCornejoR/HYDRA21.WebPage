---
// CallToAction.astro - Componente motivacional con icosahedron
---

<section class="relative overflow-hidden bg-gradient-to-br from-gray-900 via-black to-blue-900/80 py-16 sm:py-20 lg:py-24">
  <!-- Smooth transition gradients for better integration -->
  <div class="absolute top-0 left-0 right-0 h-20 bg-gradient-to-b from-black via-black/80 to-transparent z-5"></div>
  <div class="absolute bottom-0 left-0 right-0 h-20 bg-gradient-to-t from-black/95 via-black/60 to-transparent z-5"></div>
  
  <!-- Grid background pattern -->
  <div class="absolute inset-0 opacity-20">
    <div class="grid-pattern"></div>
  </div>
  
  <!-- Decorative elements -->
  <div class="absolute top-10 left-10 w-32 h-32 bg-blue-500/10 rounded-full blur-xl animate-pulse-slow"></div>
  <div class="absolute bottom-10 right-10 w-48 h-48 bg-cyan-500/10 rounded-full blur-2xl animate-pulse-slow" style="animation-delay: -3s;"></div>
  
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="flex flex-col lg:flex-row items-center justify-between gap-12 lg:gap-16">
      
      <!-- Text Content -->
      <div class="flex-1 text-center lg:text-left space-y-4 sm:space-y-6" transition:name="cta-content">
        <h2 class="text-xl xs:text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-bold text-white leading-tight" transition:name="cta-title">
          ¡Es hora de
          <span class="gradient-text-blue-cyan">revolucionar</span>
          tu ingeniería!
        </h2>

        <p class="text-sm xs:text-base sm:text-lg md:text-xl text-gray-300 leading-relaxed max-w-2xl" transition:name="cta-description">
          No esperes más para experimentar el poder de HYDRA²¹. Únete a miles de ingenieros que ya han transformado su flujo de trabajo con nuestras herramientas de vanguardia.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center lg:justify-start" transition:name="cta-buttons">
          <a
            href="/descargas"
            class="group inline-flex items-center justify-center px-6 xs:px-7 sm:px-8 py-3 xs:py-3.5 sm:py-4 text-sm xs:text-base sm:text-lg font-semibold text-white bg-gradient-to-r from-blue-600 to-cyan-600 rounded-lg hover:from-blue-500 hover:to-cyan-500 transition-all duration-300 hover:scale-105 hover:shadow-2xl hover:shadow-blue-500/25 min-h-[44px]"
          >
            Descargar Ahora
            <svg class="ml-2 w-4 h-4 xs:w-5 xs:h-5 group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
          </a>

          <a
            href="#caracteristicas"
            class="group inline-flex items-center justify-center px-6 xs:px-7 sm:px-8 py-3 xs:py-3.5 sm:py-4 text-sm xs:text-base sm:text-lg font-semibold text-gray-300 border-2 border-gray-600 rounded-lg hover:border-cyan-400 hover:text-white transition-all duration-300 hover:bg-gray-800/50 min-h-[44px]"
          >
            Ver Características
            <svg class="ml-2 w-4 h-4 xs:w-5 xs:h-5 group-hover:scale-110 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
            </svg>
          </a>
        </div>
        
        <!-- Stats or trust indicators -->
        <div class="flex flex-wrap gap-4 xs:gap-6 sm:gap-8 justify-center lg:justify-start pt-4 sm:pt-6" transition:name="cta-stats">
          <div class="text-center">
            <div class="text-lg xs:text-xl sm:text-2xl font-bold text-cyan-400">10K+</div>
            <div class="text-xs xs:text-sm text-gray-400">Proyectos Completados</div>
          </div>
          <div class="text-center">
            <div class="text-lg xs:text-xl sm:text-2xl font-bold text-blue-400">50+</div>
            <div class="text-xs xs:text-sm text-gray-400">Países</div>
          </div>
          <div class="text-center">
            <div class="text-lg xs:text-xl sm:text-2xl font-bold text-purple-400">99.9%</div>
            <div class="text-xs xs:text-sm text-gray-400">Tiempo Activo</div>
          </div>
        </div>
      </div>
      
      <!-- Icosahedron Image -->
      <div class="flex-shrink-0 relative" transition:name="cta-icosahedron">
        <div class="relative">
          <!-- Glow effect behind the image -->
          <div class="absolute inset-0 bg-gradient-to-r from-blue-500/30 via-cyan-500/40 to-purple-500/30 rounded-full blur-xl scale-110"></div>

          <!-- Main image with continuous rotation -->
          <img
            src="/assets/images/icosahedron_2.png"
            alt="HYDRA²¹ Icosahedron"
            class="relative z-10 w-48 h-48 xs:w-56 xs:h-56 sm:w-64 sm:h-64 md:w-72 md:h-72 lg:w-80 lg:h-80 xl:w-96 xl:h-96 object-contain drop-shadow-2xl animate-spin-slow"
            transition:name="icosahedron-image"
          />

          <!-- Floating particles effect -->
          <div class="absolute top-1/4 left-1/4 w-1.5 h-1.5 xs:w-2 xs:h-2 bg-cyan-400 rounded-full animate-ping opacity-75"></div>
          <div class="absolute top-3/4 right-1/4 w-1 h-1 xs:w-1.5 xs:h-1.5 bg-blue-400 rounded-full animate-ping opacity-75" style="animation-delay: -1s;"></div>
          <div class="absolute bottom-1/4 left-3/4 w-1 h-1 bg-purple-400 rounded-full animate-ping opacity-75" style="animation-delay: -2s;"></div>
          <div class="absolute top-1/2 right-1/2 w-1 h-1 xs:w-1.5 xs:h-1.5 bg-emerald-400 rounded-full animate-ping opacity-50" style="animation-delay: -0.5s;"></div>
        </div>
      </div>
      
    </div>
  </div>
</section>

<style>
  /* Grid pattern background - ORIGINAL SIN ANIMACIÓN */
  .grid-pattern {
    background-image: 
      linear-gradient(rgba(59, 130, 246, 0.1) 1px, transparent 1px),
      linear-gradient(90deg, rgba(59, 130, 246, 0.1) 1px, transparent 1px);
    background-size: 40px 40px;
    background-position: 0 0, 0 0;
    width: 100%;
    height: 100%;
    animation: grid-move 20s linear infinite;
  }
  
  @keyframes grid-move {
    0% {
      transform: translate(0, 0);
    }
    100% {
      transform: translate(40px, 40px);
    }
  }
  
  /* Animación de rotación continua simple para icosahedron */
  @keyframes spin-slow {
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(360deg);
    }
  }
  
  .animate-spin-slow {
    animation: spin-slow 20s linear infinite;
  }
  
  /* Enhanced gradient text */
  .gradient-text-blue-cyan {
    background: linear-gradient(135deg, #60a5fa 0%, #06b6d4 50%, #8b5cf6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    filter: drop-shadow(0 0 12px rgba(59, 130, 246, 0.4));
  }
  
  /* Custom pulse animation */
  @keyframes pulse-slow {
    0%, 100% {
      opacity: 0.3;
      transform: scale(1);
    }
    50% {
      opacity: 0.8;
      transform: scale(1.05);
    }
  }
  
  .animate-pulse-slow {
    animation: pulse-slow 4s ease-in-out infinite;
  }
  
  /* Enhanced smooth transitions for section integration */
  section {
    position: relative;
  }
  
  /* Ensure seamless blending with adjacent sections */
  section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent 0%, rgba(59, 130, 246, 0.3) 50%, transparent 100%);
    z-index: 20;
  }
  
  section::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent 0%, rgba(6, 182, 212, 0.3) 50%, transparent 100%);
    z-index: 20;
  }
  
  /* Responsive adjustments */
  @media (max-width: 640px) {
    .grid-pattern {
      background-size: 20px 20px;
    }
    
    @keyframes grid-move {
      0% {
        transform: translate(0, 0);
      }
      100% {
        transform: translate(20px, 20px);
      }
    }
    
    /* Slower rotation on mobile for better performance */
    .animate-spin-slow {
      animation: spin-slow 30s linear infinite;
    }
  }
</style> 