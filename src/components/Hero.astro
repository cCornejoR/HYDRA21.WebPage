---
// Brand New Modern Hero Section for HYDRA²¹
---

<section class="hero-section relative min-h-screen flex items-center overflow-hidden">
  <!-- Background Image - Responsive -->
  <div class="absolute inset-0 bg-black hero-background">
    <div class="absolute inset-0 bg-black/40 xs:bg-black/30 sm:bg-black/25 md:bg-black/20 lg:bg-black/30"></div>
  </div>
  
  <!-- Bottom blur transition - Responsive heights -->
  <div class="absolute bottom-0 left-0 right-0 h-20 sm:h-24 md:h-32 lg:h-40 bg-gradient-to-t from-black via-black/80 to-transparent z-20"></div>
  <div class="absolute bottom-0 left-0 right-0 h-16 sm:h-20 md:h-24 lg:h-32 bg-gradient-to-t from-black/90 to-transparent z-21"></div>
  <div class="absolute bottom-0 left-0 right-0 h-12 sm:h-14 md:h-16 lg:h-20 bg-gradient-to-t from-black to-transparent z-22"></div>
  
  <!-- Geometric grid pattern - Enhanced responsive -->
  <div class="absolute inset-0 opacity-8 xs:opacity-10 sm:opacity-12 md:opacity-16 lg:opacity-20">
    <div class="absolute inset-0 grid-pattern"></div>
  </div>
  
  <!-- 45-degree AutoCAD-style blueprint grid - Enhanced responsive -->
  <div class="absolute inset-0 opacity-15 xs:opacity-18 sm:opacity-20 md:opacity-24 lg:opacity-28">
    <div class="autocad-grid blur-edges"></div>
  </div>  

  <!-- Main content container - Enhanced responsive padding -->
  <div class="container mx-auto px-2 xs:px-3 sm:px-4 md:px-6 lg:px-8 xl:px-12 relative z-10 py-4 xs:py-6 sm:py-8 md:py-12 lg:py-16 xl:py-0">
    <div class="grid lg:grid-cols-2 gap-4 xs:gap-6 sm:gap-8 md:gap-10 lg:gap-12 xl:gap-16 items-center">
      <!-- Left content - Enhanced responsive spacing -->
      <div class="space-y-3 xs:space-y-4 sm:space-y-6 md:space-y-8 text-center lg:text-left">
        <!-- Badge - Responsive sizing -->
        <div class="inline-flex items-center gap-1 xs:gap-2 bg-gray-900/90 backdrop-blur-sm border border-blue-500/30 rounded-full px-2 xs:px-3 py-1 xs:py-2 text-xs">
          <span class="text-orange-400">⚡</span>
          <span class="text-blue-400 font-medium text-xs xs:text-sm">#SOFTWARE DE INGENIERÍA HIDRÁULICA</span>
        </div>
        
        <!-- Main heading - Enhanced responsive typography -->
        <h1 class="text-xl xs:text-2xl sm:text-3xl md:text-4xl lg:text-5xl xl:text-6xl 2xl:text-7xl font-bold text-white leading-tight">
          Diseña y analiza,
          <span class="block text-blue-400">haz ingeniería de</span>
          <span class="block text-blue-300">forma fácil</span>
        </h1>
        
        <!-- Subtitle - Enhanced responsive sizing -->
        <p class="text-sm xs:text-base sm:text-lg md:text-xl lg:text-2xl text-gray-300 leading-relaxed max-w-lg mx-auto lg:mx-0">
          Donde los Desafíos de Ingeniería<br class="hidden sm:block"/>
          Encuentran Soluciones Innovadoras.
        </p>
        
        <!-- Stats - Enhanced responsive grid -->
        <div class="grid grid-cols-3 gap-2 xs:gap-3 sm:gap-4 md:gap-6 lg:gap-8 pt-3 xs:pt-4 sm:pt-6 md:pt-8">
          <div class="text-center">
            <div class="text-lg xs:text-xl sm:text-2xl md:text-3xl lg:text-4xl font-bold text-blue-400">99.7%</div>
            <div class="text-xs xs:text-xs sm:text-sm text-gray-400 uppercase tracking-wider">PRECISIÓN</div>
          </div>
          <div class="text-center">
            <div class="text-lg xs:text-xl sm:text-2xl md:text-3xl lg:text-4xl font-bold text-blue-400">50x</div>
            <div class="text-xs xs:text-xs sm:text-sm text-gray-400 uppercase tracking-wider">MÁS RÁPIDO</div>
          </div>
          <div class="text-center">
            <div class="text-lg xs:text-xl sm:text-2xl md:text-3xl lg:text-4xl font-bold text-blue-400">ISO</div>
            <div class="text-xs xs:text-xs sm:text-sm text-gray-400 uppercase tracking-wider">CERTIFICADO</div>
          </div>
        </div>
        
        <!-- CTA Button - Enhanced responsive sizing -->
        <div class="pt-2 xs:pt-3 sm:pt-4 md:pt-6">
          <button class="bg-blue-600 hover:bg-blue-700 text-white px-4 xs:px-5 sm:px-6 md:px-8 py-2 xs:py-3 sm:py-4 rounded-lg text-xs xs:text-sm sm:text-base md:text-lg font-semibold transition-all duration-300 transform hover:scale-105 hover:shadow-lg hover:shadow-blue-500/25 w-full sm:w-auto">
            EXPLORAR CARACTERÍSTICAS →
          </button>
        </div>
      </div>
      
      <!-- Right side - Enhanced responsive 3D Shapes cluster -->
      <div class="relative flex justify-center lg:justify-end mt-4 xs:mt-6 sm:mt-8 lg:mt-0">
        <div class="shapes-cluster relative w-64 xs:w-72 sm:w-80 md:w-96 lg:w-[28rem] xl:w-[32rem] 2xl:w-[36rem] h-64 xs:h-72 sm:h-80 md:h-96 lg:h-[28rem] xl:h-[32rem] 2xl:h-[36rem]">
          <!-- Main cubes with enhanced responsive sizing -->
          <div class="absolute cube-cluster-2 top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2">
            <img src="/assets/images/cube.png" alt="3D Cube" class="w-32 h-32 sm:w-40 sm:h-40 md:w-48 md:h-48 lg:w-64 lg:h-64 xl:w-72 xl:h-72 2xl:w-80 2xl:h-80 animate-float-2" />
          </div>
          <div class="absolute cube-cluster-3 top-1/3 left-1/3 transform -translate-x-1/4 -translate-y-1/4">
            <img src="/assets/images/cube.png" alt="3D Cube" class="w-28 h-28 sm:w-32 sm:h-32 md:w-40 md:h-40 lg:w-52 lg:h-52 xl:w-60 xl:h-60 2xl:w-64 2xl:h-64 animate-float-3" />
          </div>
          <div class="absolute cube-cluster-4 top-2/3 left-2/3 transform -translate-x-3/4 -translate-y-3/4">
            <img src="/assets/images/cube.png" alt="3D Cube" class="w-24 h-24 sm:w-28 sm:h-28 md:w-32 md:h-32 lg:w-44 lg:h-44 xl:w-52 xl:h-52 2xl:w-56 2xl:h-56 animate-float-4" />
          </div> 
        </div>
      </div>
    </div>
  </div>
  
<style>
  .hero-section {
    background: #000000;
  }
  
  /* Hero background image - Enhanced responsive */
  .hero-background {
    background-image: url('/assets/images/fondo_2.jpeg');
    background-size: cover;
    background-position: center center;
    background-repeat: no-repeat;
    opacity: 0.5;
    background-attachment: scroll;
  }
  
  /* Mobile specific background optimizations */
  @media (max-width: 767px) {
    .hero-background {
      opacity: 0.9;
      background-size: cover;
      background-position: center center;
      background-attachment: scroll;
    }
  }
  
  @media (min-width: 768px) {
    .hero-background {
      background-attachment: fixed;
      opacity: 0.7;
    }
  }
  
  /* Grid pattern - Fixed size for all screens */
  .grid-pattern {
    background-image: radial-gradient(circle at 1px 1px, rgba(255,255,255,0.15) 1px, transparent 0);
    background-size: 35px 35px; /* Fixed size - no scaling */
  }
  
  @media (min-width: 768px) {
    .hero-background {
      background-attachment: fixed;
    }
  }
  
  /* Mobile optimizations - No grid scaling */
  @media (max-width: 499px) {
    /* Extra small screens optimization */
    .hero-background {
      background-attachment: scroll;
      background-position: center center;
      background-size: cover;
      /* Enhanced filters for extra small screens */
      filter: brightness(1.2) contrast(1.2) saturate(1.1);
      opacity: 0.95;
    }
    
    .hero-section {
      min-height: 100svh;
    }
    
    /* Enhanced text shadows for extra small screens */
    .hero-section h1 {
      text-shadow: 0 3px 12px rgba(0, 0, 0, 0.9);
    }
    
    .hero-section p {
      text-shadow: 0 2px 8px rgba(0, 0, 0, 0.8);
    }
    
    /* Floating shapes optimization for very small screens */
    .floating-shapes {
      opacity: 0.18;
    }
    
    .floating-cube img {
      width: 50px;
      height: 50px;
      opacity: 1;
    }
  }
  
  @media (min-width: 500px) and (max-width: 767px) {
    /* Regular mobile optimization - No grid scaling */
    .hero-background {
      background-attachment: scroll;
      background-position: center center;
      background-size: cover;
      filter: brightness(1.1) contrast(1.1);
    }
    
    .hero-section {
      min-height: 100svh;
    }
  }
  
  /* Bottom blur effect for footer integration */
  .hero-section::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 120px;
    background: linear-gradient(
      to top,
      rgba(0, 0, 0, 1) 0%,
      rgba(0, 0, 0, 0.95) 20%,
      rgba(0, 0, 0, 0.8) 40%,
      rgba(0, 0, 0, 0.6) 60%,
      rgba(0, 0, 0, 0.3) 80%,
      transparent 100%
    );
    z-index: 25;
    pointer-events: none;
  }
  
  /* Additional blur layer for smoother transition */
  .hero-section::before {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 80px;
    background: linear-gradient(
      to top,
      rgba(0, 0, 0, 0.9) 0%,
      rgba(0, 0, 0, 0.7) 30%,
      rgba(0, 0, 0, 0.4) 60%,
      transparent 100%
    );
    backdrop-filter: blur(2px);
    z-index: 24;
    pointer-events: none;
  }
  
  /* Responsive blur adjustments */
  @media (max-width: 640px) {
    .hero-section::after {
      height: 80px;
    }
    
    .hero-section::before {
      height: 60px;
      backdrop-filter: blur(1px);
    }
  }
  
  @media (min-width: 1024px) {
    .hero-section::after {
      height: 150px;
    }
    
    .hero-section::before {
      height: 100px;
      backdrop-filter: blur(3px);
    }
  }
  
  /* Floating background shapes */
  .floating-shapes {
    position: absolute;
    width: 100%;
    height: 100%;
    opacity: 0.12;
  }
  
  .floating-cube {
    position: absolute;
    animation-duration: 20s;
    animation-iteration-count: infinite;
    animation-timing-function: ease-in-out;
  }
  
  .floating-cube img {
    width: 80px;
    height: 80px;
    filter: hue-rotate(180deg) saturate(2) brightness(1.5);
    opacity: 0.8;
  }
  
  @media (max-width: 640px) {
    .floating-shapes {
      opacity: 0.15;
    }
    
    .floating-cube img {
      width: 60px;
      height: 60px;
      opacity: 0.9;
    }
  }
  
  @media (min-width: 641px) and (max-width: 768px) {
    .floating-cube img {
      width: 70px;
      height: 70px;
    }
  }
  
  @media (min-width: 769px) and (max-width: 1024px) {
    .floating-cube img {
      width: 90px;
      height: 90px;
    }
  }
  
  @media (min-width: 1024px) {
    .floating-cube img {
      width: 130px;
      height: 130px;
    }
  }
  
  .cube-1 {
    top: 10%;
    left: 5%;
    animation-name: float-diagonal-1;
    animation-delay: 0s;
  }
  
  .cube-2 {
    top: 20%;
    right: 10%;
    animation-name: float-diagonal-2;
    animation-delay: -3s;
  }
  
  .cube-3 {
    top: 60%;
    left: 15%;
    animation-name: float-vertical;
    animation-delay: -6s;
  }
  
  .cube-5 {
    top: 70%;
    left: 8%;
    animation-name: float-diagonal-1;
    animation-delay: -12s;
  }
  
  .cube-6 {
    bottom: 20%;
    right: 8%;
    animation-name: float-diagonal-2;
    animation-delay: -15s;
  }
  
  .cube-7 {
    top: 15%;
    left: 50%;
    animation-name: float-vertical;
    animation-delay: -18s;
  }
  
  .cube-8 {
    bottom: 15%;
    left: 45%;
    animation-name: float-horizontal;
    animation-delay: -21s;
  }
  
  
  /* Main cluster animations - Enhanced and smoother */
  @keyframes float-1 {
    0%, 100% { 
      transform: translateY(0px) translateX(0px) rotate(0deg) scale(1); 
      filter: hue-rotate(0deg) brightness(1);
    }
    25% { 
      transform: translateY(-12px) translateX(4px) rotate(90deg) scale(1.05); 
      filter: hue-rotate(90deg) brightness(1.2);
    }
    50% { 
      transform: translateY(-8px) translateX(0px) rotate(180deg) scale(1.1); 
      filter: hue-rotate(180deg) brightness(1.4);
    }
    75% { 
      transform: translateY(-16px) translateX(-4px) rotate(270deg) scale(1.05); 
      filter: hue-rotate(270deg) brightness(1.2);
    }
  }
  
  @keyframes float-2 {
    0%, 100% { 
      transform: translateY(0px) rotate(0deg) scale(1); 
      filter: hue-rotate(0deg) brightness(1) saturate(1);
    }
    20% { 
      transform: translateY(-18px) rotate(72deg) scale(1.15); 
      filter: hue-rotate(72deg) brightness(1.3) saturate(1.5);
    }
    40% { 
      transform: translateY(-10px) rotate(144deg) scale(0.95); 
      filter: hue-rotate(144deg) brightness(1.1) saturate(1.2);
    }
    60% { 
      transform: translateY(-22px) rotate(216deg) scale(1.2); 
      filter: hue-rotate(216deg) brightness(1.4) saturate(1.8);
    }
    80% { 
      transform: translateY(-6px) rotate(288deg) scale(1.05); 
      filter: hue-rotate(288deg) brightness(1.2) saturate(1.3);
    }
  }
  
  @keyframes float-3 {
    0%, 100% { 
      transform: translateY(0px) translateX(0px) rotate(0deg) scale(1); 
      filter: hue-rotate(0deg) brightness(1);
    }
    33% { 
      transform: translateY(-14px) translateX(8px) rotate(120deg) scale(1.1); 
      filter: hue-rotate(120deg) brightness(1.3);
    }
    66% { 
      transform: translateY(-20px) translateX(-6px) rotate(240deg) scale(0.9); 
      filter: hue-rotate(240deg) brightness(1.5);
    }
  }
  
  @keyframes float-4 {
    0%, 100% { 
      transform: translateY(0px) rotate(0deg) scale(1); 
      filter: hue-rotate(0deg) brightness(1);
    }
    30% { 
      transform: translateY(-25px) rotate(108deg) scale(1.25); 
      filter: hue-rotate(108deg) brightness(1.4);
    }
    70% { 
      transform: translateY(-15px) rotate(252deg) scale(1.1); 
      filter: hue-rotate(252deg) brightness(1.2);
    }
  }
  
  /* Background floating animations - More dynamic */
  @keyframes float-diagonal-1 {
    0% { 
      transform: translate(0, 0) rotate(0deg) scale(1); 
      opacity: 0.3;
    }
    25% { 
      transform: translate(60px, -40px) rotate(90deg) scale(1.2); 
      opacity: 0.6;
    }
    50% { 
      transform: translate(30px, -80px) rotate(180deg) scale(0.8); 
      opacity: 0.9;
    }
    75% { 
      transform: translate(-40px, -50px) rotate(270deg) scale(1.1); 
      opacity: 0.4;
    }
    100% { 
      transform: translate(0, 0) rotate(360deg) scale(1); 
      opacity: 0.3;
    }
  }
  
  @keyframes float-diagonal-2 {
    0% { 
      transform: translate(0, 0) rotate(0deg) scale(1); 
      opacity: 0.4;
    }
    30% { 
      transform: translate(-50px, -30px) rotate(108deg) scale(1.3); 
      opacity: 0.7;
    }
    60% { 
      transform: translate(-20px, -70px) rotate(216deg) scale(0.9); 
      opacity: 0.8;
    }
    90% { 
      transform: translate(40px, -40px) rotate(324deg) scale(1.15); 
      opacity: 0.5;
    }
    100% { 
      transform: translate(0, 0) rotate(360deg) scale(1); 
      opacity: 0.4;
    }
  }
  
  @keyframes float-vertical {
    0%, 100% { 
      transform: translateY(0px) rotate(0deg) scale(1); 
      opacity: 0.3;
    }
    50% { 
      transform: translateY(-60px) rotate(180deg) scale(1.4); 
      opacity: 0.8;
    }
  }
  
  @keyframes float-horizontal {
    0%, 100% { 
      transform: translateX(0px) rotate(0deg) scale(1); 
      opacity: 0.4;
    }
    50% { 
      transform: translateX(40px) rotate(180deg) scale(1.2); 
      opacity: 0.7;
    }
  }
  
  .animate-float-1 {
    animation: float-1 8s ease-in-out infinite;
    filter: hue-rotate(200deg) saturate(2) brightness(1.5);
  }
  
  .animate-float-2 {
    animation: float-2 10s ease-in-out infinite;
    animation-delay: -2.5s;
    filter: hue-rotate(240deg) saturate(2.5) brightness(1.8);
  }
  
  .animate-float-3 {
    animation: float-3 9s ease-in-out infinite;
    animation-delay: -5s;
    filter: hue-rotate(180deg) saturate(2.2) brightness(1.6);
  }
  
  .animate-float-4 {
    animation: float-4 7s ease-in-out infinite;
    animation-delay: -1.5s;
    filter: hue-rotate(220deg) saturate(2.8) brightness(2);
  }
  
  /* Responsive adjustments */
  @media (max-width: 640px) {
    .shapes-cluster {
      width: 400px !important;
      height: 400px !important;
    }
    
    .floating-cube img {
      width: 55px !important;
      height: 55px !important;
    }
    
    /* Reduce animation intensity on mobile */
    @keyframes float-1 {
      0%, 100% { 
        transform: translateY(0px) translateX(0px) rotate(0deg) scale(1); 
        filter: hue-rotate(0deg) brightness(1);
      }
      50% { 
        transform: translateY(-8px) translateX(0px) rotate(180deg) scale(1.05); 
        filter: hue-rotate(180deg) brightness(1.2);
      }
    }
    
    @keyframes float-2 {
      0%, 100% { 
        transform: translateY(0px) rotate(0deg) scale(1); 
        filter: hue-rotate(0deg) brightness(1);
      }
      50% { 
        transform: translateY(-12px) rotate(180deg) scale(1.1); 
        filter: hue-rotate(180deg) brightness(1.3);
      }
    }
    
    @keyframes float-3 {
      0%, 100% { 
        transform: translateY(0px) translateX(0px) rotate(0deg) scale(1); 
        filter: hue-rotate(0deg) brightness(1);
      }
      50% { 
        transform: translateY(-10px) translateX(4px) rotate(180deg) scale(1.05); 
        filter: hue-rotate(180deg) brightness(1.2);
      }
    }
    
    @keyframes float-4 {
      0%, 100% { 
        transform: translateY(0px) rotate(0deg) scale(1); 
        filter: hue-rotate(0deg) brightness(1);
      }
      50% { 
        transform: translateY(-15px) rotate(180deg) scale(1.1); 
        filter: hue-rotate(180deg) brightness(1.2);
      }
    }
  }
  
  @media (max-width: 768px) {
    .shapes-cluster {
      width: 480px;
      height: 480px;
    }
    
    .floating-cube img {
      width: 70px;
      height: 70px;
    }
    
    /* Reduce AutoCAD grid complexity on tablets */
    .autocad-grid {
      background-size: 
        30px 30px,
        30px 30px,
        15px 15px,
        15px 15px;
    }
  }
  
  @media (min-width: 1024px) {
    .shapes-cluster {
      width: 800px;
      height: 800px;
    }
  }
  
  @media (min-width: 1280px) {
    .shapes-cluster {
      width: 960px;
      height: 960px;
    }
  }
  
  @media (min-width: 1536px) {
    .shapes-cluster {
      width: 1152px;
      height: 1152px;
    }
  }
  
  /* AutoCAD-style 45-degree blueprint grid - Fixed size */
  .autocad-grid {
    width: 100%;
    height: 100%;
    background-image: 
      /* Primary diagonal lines at 45 degrees */
      linear-gradient(45deg, transparent 48%, rgba(0, 150, 255, 0.3) 48%, rgba(0, 150, 255, 0.3) 52%, transparent 52%),
      /* Secondary diagonal lines at -45 degrees */
      linear-gradient(-45deg, transparent 48%, rgba(0, 120, 255, 0.25) 48%, rgba(0, 120, 255, 0.25) 52%, transparent 52%),
      /* Horizontal construction lines */
      linear-gradient(0deg, transparent 49%, rgba(0, 180, 255, 0.15) 49%, rgba(0, 180, 255, 0.15) 51%, transparent 51%),
      /* Vertical construction lines */
      linear-gradient(90deg, transparent 49%, rgba(0, 180, 255, 0.15) 49%, rgba(0, 180, 255, 0.15) 51%, transparent 51%);
    background-size: 
      40px 40px,
      40px 40px,
      20px 20px,
      20px 20px; /* Fixed size - no scaling */
    background-position: 
      0 0,
      0 0,
      0 0,
      0 0;
    transform: rotate(0deg);
    filter: blur(0.1px);
  }
  
  /* Blur edges effect for the grid */
  .blur-edges {
    position: relative;
    mask: 
      linear-gradient(90deg, transparent 0%, black 15%, black 85%, transparent 100%),
      linear-gradient(0deg, transparent 0%, black 15%, black 85%, transparent 100%);
    mask-composite: intersect;
    -webkit-mask: 
      linear-gradient(90deg, transparent 0%, black 15%, black 85%, transparent 100%),
      linear-gradient(0deg, transparent 0%, black 15%, black 85%, transparent 100%);
    -webkit-mask-composite: source-in;
  }
  
  /* Additional edge blur with pseudo-elements */
  .blur-edges::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: inherit;
    filter: blur(2px);
    opacity: 0.3;
    mask: 
      linear-gradient(90deg, black 0%, transparent 20%, transparent 80%, black 100%),
      linear-gradient(0deg, black 0%, transparent 20%, transparent 80%, black 100%);
    mask-composite: intersect;
    -webkit-mask: 
      linear-gradient(90deg, black 0%, transparent 20%, transparent 80%, black 100%),
      linear-gradient(0deg, black 0%, transparent 20%, transparent 80%, black 100%);
    -webkit-mask-composite: source-in;
  }
  
  /* Alternative blueprint grid with more technical aesthetic */
  .autocad-grid::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image:
      /* Fine diagonal mesh */
      linear-gradient(45deg, rgba(0, 180, 255, 0.1) 1px, transparent 1px),
      linear-gradient(-45deg, rgba(0, 180, 255, 0.08) 1px, transparent 1px),
      /* Major grid lines every 80px */
      linear-gradient(45deg, transparent 79px, rgba(0, 150, 255, 0.4) 79px, rgba(0, 150, 255, 0.4) 81px, transparent 81px),
      linear-gradient(-45deg, transparent 79px, rgba(0, 150, 255, 0.35) 79px, rgba(0, 150, 255, 0.35) 81px, transparent 81px);
    background-size:
      15px 15px,
      15px 15px,
      80px 80px,
      80px 80px;
    opacity: 0.8;
    /* Soft blur for the detailed grid */
    filter: blur(0.3px);
    /* Edge fade for this layer */
    mask: 
      radial-gradient(ellipse at center, black 60%, transparent 100%);
    -webkit-mask: 
      radial-gradient(ellipse at center, black 60%, transparent 100%);
  }
  
  /* Engineering blueprint corner marks with edge blur */
  .blur-edges::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image:
      /* Corner registration marks */
      radial-gradient(circle at 0% 0%, rgba(0, 200, 255, 0.5) 1px, transparent 2px),
      radial-gradient(circle at 100% 0%, rgba(0, 200, 255, 0.5) 1px, transparent 2px),
      radial-gradient(circle at 0% 100%, rgba(0, 200, 255, 0.5) 1px, transparent 2px),
      radial-gradient(circle at 100% 100%, rgba(0, 200, 255, 0.5) 1px, transparent 2px);
    background-size: 60px 60px;
    background-position: 0 0, 60px 0, 0 60px, 60px 60px;
    opacity: 1;
    /* Soft blur for corner marks */
    filter: blur(0.5px);
    /* Corner fade effect */
    mask: 
      radial-gradient(circle at 0% 0%, black 80%, transparent 100%),
      radial-gradient(circle at 100% 0%, black 80%, transparent 100%),
      radial-gradient(circle at 0% 100%, black 80%, transparent 100%),
      radial-gradient(circle at 100% 100%, black 80%, transparent 100%),
      linear-gradient(90deg, transparent 0%, black 10%, black 90%, transparent 100%),
      linear-gradient(0deg, transparent 0%, black 10%, black 90%, transparent 100%);
    mask-composite: intersect;
    -webkit-mask: 
      radial-gradient(circle at 0% 0%, black 80%, transparent 100%),
      radial-gradient(circle at 100% 0%, black 80%, transparent 100%),
      radial-gradient(circle at 0% 100%, black 80%, transparent 100%),
      radial-gradient(circle at 100% 100%, black 80%, transparent 100%),
      linear-gradient(90deg, transparent 0%, black 10%, black 90%, transparent 100%),
      linear-gradient(0deg, transparent 0%, black 10%, black 90%, transparent 100%);
    -webkit-mask-composite: source-in;
  }
  
  /* Additional responsive optimizations */
  @media (max-width: 640px) {
    /* Optimize performance on mobile */
    .floating-cube {
      animation-duration: 25s; /* Slower animations on mobile */
      will-change: transform;
    }
    
    /* Reduce blur effects to improve performance */
    .hero-section::before {
      backdrop-filter: none;
    }
    
    /* Ensure text remains readable */
    .hero-section h1 {
      text-shadow: 0 2px 10px rgba(0, 0, 0, 0.8);
    }
    
    .hero-section p {
      text-shadow: 0 1px 6px rgba(0, 0, 0, 0.6);
    }
  }
  
  @media (max-width: 480px) {
    /* Extra small mobile devices - No grid scaling */
    .shapes-cluster {
      width: 240px !important;
      height: 240px !important;
    }
    
    /* Maximum background visibility for tiny screens */
    .hero-background {
      opacity: 1 !important;
      filter: brightness(1.3) contrast(1.3) saturate(1.2) !important;
    }
    
    /* Enhance floating shapes for tiny screens */
    .floating-shapes {
      opacity: 0.2 !important;
    }
    
    .floating-cube img {
      width: 45px !important;
      height: 45px !important;
      opacity: 1 !important;
      filter: hue-rotate(180deg) saturate(3) brightness(2) !important;
    }
    
    /* Maximum text contrast */
    .hero-section h1 {
      text-shadow: 0 4px 16px rgba(0, 0, 0, 1) !important;
    }
    
    .hero-section p {
      text-shadow: 0 3px 12px rgba(0, 0, 0, 0.9) !important;
    }
    
    /* Ensure overlay doesn't hide background */
    .hero-section .absolute.inset-0.bg-black\\/40 {
      background: rgba(0, 0, 0, 0.2) !important;
    }
  }
  
  @media (max-width: 400px) {
    /* Ultra small screens */
    .hero-background {
      opacity: 1 !important;
      filter: brightness(1.4) contrast(1.4) saturate(1.3) !important;
    }
    
    .grid-pattern {
      background-size: 12px 12px !important;
      background-image: radial-gradient(circle at 1px 1px, rgba(0, 150, 255, 0.6) 1px, transparent 0) !important;
    }
    
    .autocad-grid {
      background-size: 
        14px 14px,
        14px 14px,
        7px 7px,
        7px 7px !important;
    }
    
    .shapes-cluster {
      width: 200px !important;
      height: 200px !important;
    }
  }
  
  /* Improve text gradients */
  .gradient-text-blue-cyan {
    background: linear-gradient(135deg, #60a5fa 0%, #06b6d4 50%, #3b82f6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-shadow: none;
    filter: drop-shadow(0 0 8px rgba(59, 130, 246, 0.3));
  }
  
  /* Enhanced mobile touch targets */
  @media (max-width: 640px) {
    button {
      min-height: 48px;
      font-size: 0.9rem;
    }
  }
  
  /* Landscape mobile optimization */
  @media (max-height: 500px) and (orientation: landscape) {
    .hero-section {
      min-height: 100vh;
      padding-top: 1rem;
      padding-bottom: 1rem;
    }
    
    .hero-section .container {
      padding-top: 2rem;
      padding-bottom: 2rem;
    }
  }
</style>
